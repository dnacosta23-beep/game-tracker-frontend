
# Game-Tracker-Frontend

## What this app does

This React app displays player records from the Flask API and lets the user add a new player.

Each player record includes:
- `username`
- `email`
- `game_name`

## How to run the frontend

1. Open the frontend folder in the terminal.
2. Install dependencies:

```bash
npm install